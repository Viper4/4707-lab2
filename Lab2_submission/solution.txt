Students:
1. Zach Karsko (karsk017)
2. Lucas Giebler (giebl007)
3. Ayden DeBoer (deboer190)

Brief Summary:<A Brief Summary about the change you make (1 Paragraph max)>
	A brief summary of the changes we made is we added a last accessed variable to each BufferDesc so we can track when each buffer was last accessed for the Top 10 LRU replacement. Then we had to change the StrategyGetBuffer so it evicts based on the top 10 LRU buffers and not clock sweep. We changed the last part of the StrategyGetBuffer to check all buffer descriptions and get the top 10 last accessed buffers and evict the buffer based on what the top 10 counter is at that time. We also had to modify the BufferAlloc in bufmgr to update the last accessed variable if the page was found in memory.

Detailed Changes:
1. buf_internals.h: postgresql-16.0/src/include/storage
	- <Detailed changes of the file>.
	In line 257 we added a variable last_accessed to track when the buffer was last used to determine what buffer is going to be evicted in the Top 10 LRU. Then we added function definitions in lines 260-265 to access the Top10LRU_count and the GlobalTimestamp as well as adjust their values.
	
2. buf_init.c: postgresql-16.0/src/backend/storage/buffer
	- <Detailed changes of the file>.
	In line 129 we initialize the last_accessed to 0. Since InitBufferPool initialize shared buffer pool we want to initialize the last_accessed to 0.

3. bufmgr.c: postgresql-16.0/src/backend/storage/buffer
	- <Detailed changes of the file>.
	In line 1280 of the function BufferAlloc we update the last accessed to the global timestamp since the buffer was found in the buffer pool so the buffer is being accessed so we need to update the last accessed time for the top 10 LRU replacement has accurate info. Then in line 1281 we need to increment the global timestamp so it is accurate to update the next buffer.

4. freelist.c: postgresql-16.0/src/backend/storage/buffer
	- <Detailed changes of the file>.
	In line 63, we added the variable Top10LRU_count to track the index of the buffer that needs to be evicted based on the Top 10 LRU description. Then, in line 64, we added the variable GlobalTimestamp, which is a monotonically increasing counter used to assign a timestamp to each buffer last_accessed when accessed. These variables were added as pg_atomic_uints so no problems are caused by multiple processes reading/writing them, like a race condition. They were placed inside BufferStrategyControl, so they reside in shared memory and are visible to all processes, ensuring consistency across the system. The accessor functions on lines 70-102 encapsulate access to the GlobalTimestamp and Top10LRU_count variables, allowing safe and controlled use from other modules such as bufmgr.c. Lines 767-768 initialize the shared memory GlobalTimestamp and Top10LRU_count uints. GlobalTimestamp was a uint64 to avoid overflow issues, Top10LRU_count was a uint32 since its values are only ever in the range 0-9.

	Then we had to adjust the function StrategyGetBuffer function to make it Top 10 LRU instead of clock sweep. In line 416, we atomically increment the global timestamp and assign the returned value to the buffer's last_accessed. Same for line 507, we want to update the last_accessed variable before it returns and uses the buffer, and we increase the global timestamp variable for the next buffer. Then, in lines 520-598, we got rid of the code used for the clock sweep algorithm and replaced it with our Top 10 LRU algorithm. First, in line 520, we needed to create an array of BufferDesc to hold the 10 potential victims. Then in line 521, we created a variable to keep track of the number of victims in case there are not 10 victims. Then line 522, we created a variable to get the index of the victim in the array. Then, in lines 524-548, we loop through all the buffers and check if it's being used. If the buffer is not being used, we check if the victim array is full. If the array is full, we check if the last_accessed time is shorter than one of the victims in the array and replace it otherwise we check the next buffer. If it's not full, we add it to the array and increase the vic_count variable. We also call LockBufHdr() and UnlockBufHdr() on the buffer, just like the clock sweep algorithm. 

	Then, in lines 551-559, we sort the victims since they can get out of order as different buffers are evicted. Then in line 563, we check if the number of victims is greater than the Top 10 LRU counter, since we might not have gotten 10 victims. If the victim count is less than the Top 10 LRU counter, we reset the counter in line 564, and the to evict index is the index of the last victim added to the array in line 565. Otherwise, we set the index that we are going to evict to the Top 10 LRU counter in line 567. Then, in lines 569-581, if the index is valid, we print the information about the buffers in the array, the buffer we are going to evict, and the Top 10 LRU counter. Then, in line 583, we increase the Top 10 LRU counter for the next time it's needed. Then lines 584-586 check if the Top 10 LRU counter reaches 10 and resets it to 0 if so. Then line 589 gets the victim. In line 591, we update the last_accessed variable and increase the GlobalTimestamp counter so it's ready to return the buffer for use. The lines 593-595 called AddBufferToRing, set buf_state to local_buf_state, and return the buffer, just like the old clock sweep algorithm. 